{
  "enable_bridge": {
    "type": "bool",
    "description": "是否启用 MCP 日志桥接服务",
    "default": true
  },
  "auth_token": {
    "type": "string",
    "description": "双向共享令牌（X-MCP-Token）",
    "hint": "客户端（MCP 宿主机）必须以 X-MCP-Token 请求头发送相同值。留空则回退读取 AstrBot 进程环境变量 ASTRBOT_LOG_MCP_TOKEN；两者皆空时不校验（不推荐，仅依赖 AstrBot API Key 鉴权）。",
    "default": ""
  },
  "log_file_path": {
    "type": "string",
    "description": "日志文件路径（可选）",
    "hint": "指定日志文件路径用于读取历史日志；留空则从内存 LogBroker 缓存读取最近 500 条。",
    "default": ""
  },
  "history_limit": {
    "type": "int",
    "description": "logs_history 单次返回的最大条数",
    "default": 200
  },
  "search_limit": {
    "type": "int",
    "description": "logs_search 单次返回的最大条数",
    "default": 200
  }
}
